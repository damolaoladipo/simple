import { ChangeEvent, RefObject } from "react";

export interface ITitle {
    text: string;
    size?: string;
    color?: string;
    margin?: {
      top?: string;
      bottom?: string;
    };
  }
  
